<section class="admin-dashboard">

<header class="page-header">
    <h1>Admin Dashboard</h1>
  </header>

  <div class="stats">
    <div class="stat">
      <h3>Total Products</h3>
      <p><%= @product_count %></p> <!-- Display the product count -->
    </div>

    <div class="stat">
      <h3>Total Categories</h3>
      <p><%= @category_count %></p> <!-- Display the category count -->
    </div>

    <div class="stat">
      <h3>Total Orders</h3>
      <p><%= @order_count %></p> <!-- Display the order count -->
    </div>
  </div>

  <%= link_to "Back to Home", root_path, class: 'btn btn-primary' %>

</section>
